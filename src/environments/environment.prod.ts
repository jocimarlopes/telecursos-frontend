export const environment = {
  production: true,

  // Endereço do deploy do backend na Vercel. O projeto lá ainda se chama
  // "telecursos-backend" — o rebrand não renomeia o deploy.
  //
  // Para migrar para um domínio próprio no futuro: adicione `api.cursando.pro`
  // ao projeto do backend na Vercel e só então troque a linha abaixo.
  API_URL: 'https://telecursos-backend.vercel.app',
};
